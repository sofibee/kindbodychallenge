<h1>
Notes History for <%= @patient.last_name %>, <%= @patient.first_name %>
</h1>

<table>
  <tr>
    <th>Text</th>
    <th></th>
    <th></th>
  </tr>

  <% @notes.each do |note| %>
    <tr>
      <td><%= note.text %></td>
      <td><%= link_to 'Edit Note', edit_patient_note_path(@patient, note) %></td>
      <td><%= link_to 'Delete Note', patient_note_path(@patient, note), method: :delete, data: { confirm: 'Are you sure?'} %></td>
    </tr>
  <% end %>
</table><br>

<%= link_to 'Add A New Note', new_patient_note_path(@patient) %><br>

<%= link_to 'All Patients', controller: 'patients' %>
